This is pretty dank.
So the first thing you need to do is fire up a database with the file I will provide.

Set these environment variables before you start
  export DB_URL=mongodb://localhost
  export DB_NAME=todo
  
